name: Global Asset Simulator - Auto Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      network:
        description: 'Network to deploy to'
        required: true
        default: 'sepolia'
        type: choice
        options:
          - sepolia
          - bsc
          - polygon
          - base

jobs:
  deploy:
    name: Deploy to ${{ github.event.inputs.network || 'sepolia' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Compile contracts
        run: npx hardhat compile

      - name: Deploy contract
        env:
          # Explicitly passing secrets to environment variables
          PRIVATE_KEY: ${{ secrets.PRIVATE_KEY }}
          SEPOLIA_RPC_URL: ${{ secrets.SEPOLIA_RPC_URL }}
          BSC_RPC_URL: ${{ secrets.BSC_RPC_URL }}
          POLYGON_RPC_URL: ${{ secrets.POLYGON_RPC_URL }}
          BASE_RPC_URL: ${{ secrets.BASE_RPC_URL }}
          TRUSTED_FORWARDER: ${{ secrets.TRUSTED_FORWARDER }}
        run: |
          # Use manual network input or default to sepolia
          DEPLOY_NETWORK=${{ github.event.inputs.network || 'sepolia' }}
          echo "Initiating deployment to $DEPLOY_NETWORK..."
          npx hardhat run scripts/deploy.js --network $DEPLOY_NETWORK
